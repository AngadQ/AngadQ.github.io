// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://angadq.github.io',
  //base: '/angad-kochhar.github.io',
  //base is commented as Repo name is same as GitHub username, so no need to add base path
  //GitHub username - AngadQ
  //Repo name - AngadQ
  //Website will be AngadQ.github.io
});